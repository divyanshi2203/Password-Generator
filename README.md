# Password-Generator
A simple Java password generator that creates strong, random passwords using letters, numbers, and special characters like @. Users can specify the desired password length via the command line.
